#ifndef COORD
#define COORD

struct Coord{
        double x;
        double y;
        void print (void);
	void set (double, double);
        double calc (void);
       };

#endif //COORD
